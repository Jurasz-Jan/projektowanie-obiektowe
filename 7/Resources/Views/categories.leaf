#extend("base") {
    #export("title") { Kategorie }

    #export("body") {
        <h1>Lista kategorii</h1>
        <form action="/categories" method="POST">
            <input name="title" placeholder="Nowa kategoria" required />
            <button type="submit">Dodaj</button>
        </form>

        <ul>
        #for(category in categories):
            <li>
                #(category.title) – #(category.products.count) produktów
            </li>
        #endfor
        </ul>
    }
}
